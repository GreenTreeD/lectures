Преподаватель: Витков Фёдор Андреевич
# Лекция №1

По предмету будет зачёт, всего 3 модуля => 3 ок
Лабы удалённо, загружаются в облако, на РК обязательно присутствовать

Специальности и специализации в этой предметной области:
- CG-специалист
- 3D-моделирование (Blender, 3DMax и тд), инструменты для фотограммометрии
- анимация (Maya в основном)
- разработка ПО
 -  render engine, разработка движка
 - SDK
 - разработчики игровой логики
 - сетевое программирование

Как примерно работает рендер 3д графики: 
Os <-> окна в операционной системе <-> программа <-> буфферы, которые хранят в себе rgb
**Рендеринг** - преобразования абстрактных данных 3d сцены в картинку на экране в режиме fullscreen или в конкретном окне. 
*Считается, что переключение из fullscreen медленнее, чем из оконного.* 
*Слова "отрисовка" и всякие другие лучше не использовать вместо слова "рендеринг" и вообще лучше изучать литературу на английском языке.* 

Библиотеки для 3d рендера: OpenGL, Direct3D, Vulkan

**API(Application Programming Interface)** - набор классов и функций для взаимодействия с внешним ПО.

Примеры современных движков: Unity, Unreal Engine, Godot 
## Новая тема. какая-то...
Программировать для Unreal Engine будем в Visual Studio. Сборка идёт с помощью Solution, \*.sln файл, который подтягивает все Visual Studio Project'ы. 
Статическая link-овка: все пакуется в один .exe файл
Динамическая link-овка: есть .exe файл и несколько .dll


# Лекция №2
### Основные понятия и определения в компьютерной графике
*Vertex* - вершина. Могут быть объединены *гранями* в *triangle* или лучше *polygon*.
Полигоны объединяются в *mesh*(сетка, но это не лучший перевод). Mesh объединяется в 3D модель.
3D-scene == map, level. Но надо понимать контекст использования *map* и *level*.
3D-scene - структура, имеющая некоторую систему координат (праворукую или леворукую), где расположены некоторые объекты, положение которых задаются 9 числами:  x,y,z, $\alpha_x$,$\alpha_y$, $\alpha_z$, $S_x$,$S_y$,$S_z$ (координаты, угол поворота, масштаб вдоль осей), должна быть камера(есть нормаль) (fov, aspect ratio, посмотреть, как считается горизонтальный и вертикальный fov)
